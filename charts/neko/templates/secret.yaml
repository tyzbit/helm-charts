{{- if eq "" .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "neko.fullname" . }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: "{{ $value | b64enc }}"
  {{- end }}
{{- end }}